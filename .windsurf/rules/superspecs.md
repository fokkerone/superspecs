# SuperSpecs — Windsurf Rules

SuperSpecs lifecycle: plan → propose → implement → review → wiki → complete

Skills in `.windsurf/skills/`. Use `/spec:*` commands via Cascade.
Never implement without a reviewed spec. Tests first, always.
